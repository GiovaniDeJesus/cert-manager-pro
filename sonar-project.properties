# -------------------------------------------------------
#  SonarQube Project Configuration - Python 3.12
# -------------------------------------------------------

# Project identification
sonar.projectKey=ssl-cert-manager
sonar.projectName=ssl-cert-manager
sonar.projectVersion=1.0.0

# Source code location
#sonar.sources=src

# Optional: where your tests are located
#sonar.tests=tests

# File encoding
sonar.sourceEncoding=UTF-8

# Python version (SonarQube uses this for parsing and analysis)
sonar.python.version=3.12

# Optional: If you use virtual environments or exclude folders
sonar.exclusions=**/__pycache__/**,**/*.pyc,venv/**,build/**,dist/**

# Optional: Coverage report (from pytest-cov)
# Example command to generate: pytest --cov=src --cov-report=xml:coverage.xml
#sonar.python.coverage.reportPaths=coverage.xml

# Optional: Unit test results (from pytest)
# Example command: pytest --junitxml=test-results.xml
#sonar.python.xunit.reportPath=test-results.xml

# Encoding and language specifics
sonar.language=py

# Optional: If you have documentation or config folders to ignore
# sonar.exclusions=docs/**,config/**

# -------------------------------------------------------
# Notes:
# 1. Ensure pytest and coverage reports are generated before running sonar-scanner.
# 2. Run analysis with: sonar-scanner -Dproject.settings=sonar-project.properties
# -------------------------------------------------------
